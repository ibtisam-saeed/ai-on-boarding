# The tooling's tests are a self-contained suite, separate from the Django
# project's. They import nothing from sdd_django_demo and need no Django
# settings, so they carry their own configuration rather than borrowing
# sdd_django_demo/pytest.ini.
[pytest]
python_files = test_*.py
testpaths = review/tests
